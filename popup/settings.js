/**
 * GlassBox Settings helpers (re-exported for any standalone settings page use).
 * The main settings UI lives in dashboard.js / dashboard.html.
 */

export { getSettings, saveSettings } from '../lib/storage.js';
